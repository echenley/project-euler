# A palindromic number reads the same both ways.
# The largest palindrome made from the product
# of two 2-digit numbers is 9009 = 91 × 99.

# Find the largest palindrome made from the
# product of two 3-digit numbers.

largest_pal = [0,0,0]
limit = 30

def check_pal?(num)
    rev_check = num.to_s.split('').reverse.join('')
    num.to_s == rev_check ? true : false
end

result = catch(:done) do
    for x in 0..limit
        for y in 0..x
            num = (limit-x)*(limit-y)
            if check_pal?(num) && num > largest_pal[0]
                largest_pal = [num,(limit-x),(limit-y)]
                puts "#{largest_pal[1]} x #{largest_pal[2]} = #{largest_pal[0]}"
            end
            throw :done, largest_pal if (limit-x)*(limit-1) < largest_pal[0]
        end
    end
end
