# 2520 is the smallest number that can
# be divided by each of the numbers from
# 1 to 10 without any remainder.

# What is the smallest positive number
# that is evenly divisible by all
# of the numbers from 1 to 20?

numbers = *(1..20)          # handles any array of numbers
numbers.sort!
x = numbers.length - 2
lcm = numbers[-1]

def gcd(a,b)
    remainder = a % b
    if remainder == 0
        return b
    else
        gcd(b,remainder)
    end
end

while x >= 0
    lcm = (lcm * numbers[x]) / gcd(lcm, numbers[x])
    x -= 1
end
puts "lcm(#{numbers.join(',')}) = #{lcm}"
